algorithm: {adv_estimator: agentflow_grpo, use_kl_in_reward: false}
actor_rollout_ref:
  model: {path: model/Qwen/Qwen3-0.6B, lora_rank: 0, lora_alpha: 128, target_modules: all-linear, use_remove_padding: true}
  actor: {strategy: fsdp, ppo_epochs: 2, ppo_mini_batch_size: 4, ppo_micro_batch_size_per_gpu: 1, clip_ratio_low: 0.001, clip_ratio_high: 0.003, use_kl_loss: false, entropy_coeff: 0.0, policy_loss: {loss_mode: gspo}, optim: {lr: 0.000002}}
  rollout: {name: vllm, n: 8, log_prob_micro_batch_size_per_gpu: 1, temperature: 1.2, top_p: 1.0, top_k: -1, repetition_penalty: 1.0, calculate_log_probs: true, load_format: safetensors, tensor_model_parallel_size: 1, gpu_memory_utilization: 0.45, agent: {default_agent_loop: agentflow_ticket, agent_loop_config_path: configs/agent_loops.yaml, num_workers: 8}}
data: {train_files: [data/verl/ticket/train.parquet], val_files: [data/verl/ticket/test.parquet], train_batch_size: 4, val_batch_size: 4, max_prompt_length: 2048, max_response_length: 512}
reward: {reward_model: {enable: false}}
trainer: {n_gpus_per_node: 1, nnodes: 1, logger: [console, file], project_name: agentflow_v3, experiment_name: ticket_eval, val_before_train: true, val_only: true, resume_mode: disable, total_epochs: 1, total_training_steps: 1, save_freq: -1, test_freq: -1, validation_data_dir: outputs/ticket/eval/validation, default_local_dir: outputs/ticket/eval}
agentflow: {task: ticket, frozen_base_url: "http://127.0.0.1:8000/v1", frozen_model: Qwen3-0.6B, max_time_s: 120, role_max_tokens: 512, prompt_reserve_tokens: 512, memory_views: {planner: {max_tokens: 1024}, executor: {max_tokens: 768, max_recent_entries: 3}, verifier: {max_tokens: 1024}, generator: {max_tokens: 1024}, base_generator: {max_tokens: 768, max_recent_entries: 3}}}
